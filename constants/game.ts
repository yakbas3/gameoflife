// GameOfLifeApp/constants/game.ts

/** Number of rows in the simulation grid. */
export const GRID_ROWS = 100;

/** Number of columns in the simulation grid. */
export const GRID_COLS = 100;

// Add other game-related constants here later if needed
// e.g., initial speed, default zoom, etc.